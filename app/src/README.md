This directory contains the Expo React Native app for the fullstack project.

Folder structure:
- `src/components/` for reusable UI components
- `src/screens/` for app screens
- `src/navigation/` for navigation setup

Use `npm install` in the `app` folder and `npm start` to launch Expo.
